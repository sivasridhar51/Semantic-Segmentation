# Semantic-Segmentation
Developed deep learning models (FCN, DeepLabV2, PSPNet) for semantic image segmentation using TensorFlow. Tuned hyperparameters and applied techniques like atrous convolution and pyramid pooling. Evaluated performance using IoU and pixel accuracy, with FCN achieving the best results.
